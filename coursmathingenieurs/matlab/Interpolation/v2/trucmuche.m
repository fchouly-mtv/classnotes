function f = trucmuche ( x )
    
    %f = (exp(x) - 1) .* (exp(x) + 1).^(-1);
    %f = exp(-2*x) ./ ( 1 + 2*exp(-x) ).^2;
    %f = x.^3 ./ (1+x.^2);
    %f = x .* exp(x);
    %f = x.^2 .* exp(x);
    f = atan(x);
    
    
    
    